<% page :full %>
<% content_for :body_style, "background-color: var(--sheet)" %>

<div>
  <div class="h-32">
    <div class="h-[220%] bg-center bg-cover" style="background-image: url('<%= background_url %>');"></div>
  </div>

  <div class="bg-(color:--elevated) rounded-(--radii-default) container narrow card">
    <%= yield %>
  </div>
</div>
